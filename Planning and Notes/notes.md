# Future Ideas & Revisions

- [ ] **Search Result Default Height**: Implement a default height for the search results panel when empty (e.g., `calc(var(--card-width) * 1.4 + 2rem)`).
- [ ] **Adaptive Overlap**: Dynamically adjust card overlap based on the number of cards in a stack or the available vertical space.
- [ ] **Tabbed View for Mobile**: Transition from the 3-row vertical stack to a tabbed interface (Search / Deck / Stats) on screens smaller than 768px.
- [ ] **Drag and Drop**: Implement native drag-and-drop for moving cards between boards (Main -> Side).
- [ ] **Mana Symbol Icons**: Replace colored divs with custom SVG mana icons for the search filters.
- [x] **Garbage Can**: Implement a "Garbage Can" to collect recently deleted cards. This helps mitigate accidental left clicks and provides a way to recover cards without searching again.
