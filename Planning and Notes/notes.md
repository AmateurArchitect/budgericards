# Future Ideas & Revisions

- [ ] **Mobile Audit**: Make sure the whole app functions on mobile sized screens.
- [ ] **Board Updates**: Smooth out the UX for mainboard/sideboard/maybeboard. Add a panel for the sideboard on the right that can expand.
- [ ] **Type Line Subtypes Pipeline**: Automate updating the official MTG subtypes list inside `data-preprocessor/build-database.mjs` by fetching Scryfall catalog APIs and writing the output to a centralized `vocabulary.json` file in the constants folder.
